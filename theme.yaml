# Source file for the logo.
# Upload an image with enough resolution to be used for @2x retina versions.
# This image must exist in the public/img/ directory
logo:
  image: 'logos/verity_logo.png'
  config:
    h: 72
    dpr: 2
variables: |
  $theme-primary-color: #0D3C7F;
  $theme-accent-color: #EBB42F;
  $theme-page-header-height: 20px;
  $theme-header-app-title: true;
  $theme-benchmark-color: #cc68d8;

# Extra css overrides
